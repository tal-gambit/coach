---
name: <goal-slug>
description: <one line — the goal, date, and objective>
sources: [chat]
aliases: []
---

## Goal
- Event: <name>
- Date: <date>
- Format/distance: <details>
- Objective: <finish / finish strong / compete / specific target — this
  is the thing every judgment call gets checked against>

## Constraints
- <injury flags, movements to avoid or modify, anything a doctor/PT has
  flagged — update this as things resolve or new ones appear>

## Fitness history
- <relevant training background — what they've done before, how long,
  prior blocks or races worth knowing about>

## Benchmarks
- <exercise/activity: baseline value, date established, how it was
  measured (tested vs. self-reported vs. from a data source) — this is
  what progressions like "5x5 @ 70%" get computed from. Update when
  retested, keep the old value with a date rather than overwriting
  silently, so progress is visible.>

## Current phase
- <where the athlete is in the block — base building / build / peak /
  taper / etc., and what that implies for session intensity right now>

## Milestones
- [ ] <thing that needs to happen once before race day — e.g. a full
  dress rehearsal, a specific skill practiced, a fueling protocol tested>
- [ ] <...>

## Standing rules
<Rules that emerged from experience during the block — things tried that
worked or didn't, patterns worth checking every time (e.g. "always check
warm-up length before assessing an early-session pace drop" or "don't
stack grip-intensive sessions two days running"). This is where the
skill accumulates what it's learned about THIS athlete specifically.>

## Logistics
- Memory backend: <what this and future files are actually stored in —
  built-in memory, local files, a connected Notion workspace, a git
  repo, etc. — confirmed at the start of Setup, so it isn't re-asked
  every session>
- Data sources: <what's connected — health app, manual reporting, etc.>
- Calendar convention: <how sessions get scheduled/titled, if the
  athlete has a preference>
- Communication style: <terse / detailed, any other stated preference>
